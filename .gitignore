# Dependencies
node_modules/

# SvelteKit build output
.svelte-kit/
build/

# Environment (API keys — never commit)
.env
.env.*
!.env.example

# Test output
test-results/
playwright-report/

# STT model archives (npm run stt:fetch — never commit, never into static/)
.stt-models/

# OS / editor
.DS_Store
# Cloudflare Workers local dev state
.wrangler/

# Orchestration tool state (.omo)
.omo/
