/** Fixed dwell times — NOT configurable by the user. */

/** 1.2 s after a correct answer before auto-advance to the next card. */
export const CORRECT_DWELL_MS = 1_200;

/** 2.5 s after an incorrect answer before the retry prompt replays. */
export const INCORRECT_DWELL_MS = 2_500;
